<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.doodeec.maroonfrog"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.doodeec.maroonfrog.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.doodeec.maroonfrog.ui.meal.list.MealListActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.doodeec.maroonfrog.ui.meal.detail.MealDetailActivity"
            android:label="@string/activity_meal_title"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--<meta-data
            android:name="io.fabric.ApiKey"
            android:value="" />-->
        <!--TODO add fabric key-->
    </application>

</manifest>
